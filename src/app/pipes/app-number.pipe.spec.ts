import { AppNumberPipe } from './app-number.pipe';

describe('AppNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new AppNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
