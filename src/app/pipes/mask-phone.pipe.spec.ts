import { MaskPhonePipe } from './mask-phone.pipe';

describe('MaskPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new MaskPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
